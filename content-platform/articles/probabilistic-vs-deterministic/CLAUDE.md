# Per-piece session contract — probabilistic-vs-deterministic

> This file is the session router for this article. When you (Claude)
> are working on this piece, load files in this order before producing
> any prose. Generated by the `content-pipeline` skill.

## 1. The constitution (always)

Read parent `../../CLAUDE.md` first — it routes you through the
voice constitution. The non-negotiable load order is:

1. `../../voice/style-guide.md`
2. `../../voice/brand-positioning.md`
3. `../../voice/audience-personas.md`
4. `../../content-types/blog-post.md`

## 2. This piece (always)

Read these in order:

1. `brief.md` — the spec for THIS article (locked)
2. `plan.md` — the structural outline (do not draft until this is
   signed off by the human reviewer; see tasks.md §1)
3. `tasks.md` — the workflow gates; check off as you go

## 3. Selective references (load on demand)

Pull only when the brief calls for them:

- `../../voice/product-knowledge.md` — when the piece touches
  Spaarke architecture (this piece does, lightly — the IQ Stack
  is used structurally in §3)
- `../../voice/domain-knowledge.md` — when the piece touches
  industry stats. **This piece is designed to be qualitative.**
  If a quantitative claim surfaces, source it or remove it.
- `../../voice/vocabulary.md` — consult when reaching for a word.
  Specific gravity-pull terms for this piece: "AI-powered" (banned),
  "transform" (banned), "by design" (banned as casual shorthand for
  the probabilistic property — see brief.md Voice notes).
- `../../voice/examples/good-articles.md` — pattern-match before
  writing the opening. Especially `institutional-knowledge` opening
  (the negation-then-diagnosis rhythm) and `the-iq-stack` middle
  (the side-by-side voice pattern).
- `../../voice/examples/avoid-this.md` — **always run as final-pass
  checklist** (see tasks.md §4 Polish)
- `../../voice/visual-identity.md` — when generating the hero. SVG
  is default per blog-post calibration §4.

## 4. Adjacent context

- **Campaign:** `2026-05-spaarke-launch` (within 5/11–5/31 window).
  Read `../../campaigns/2026-05-spaarke-launch.md` for the narrative
  arc this piece sits inside — picks up the IQ-stack Inference thread
  from [context-is-only-one-layer](../context-is-only-one-layer/)
  (LinkedIn post, 2026-05-18).
- **Calendar row:** `../../calendar.md` — this piece's row, for
  cadence + cross-references.
- **GitHub Issue:** set after content-pipeline Step 5; see brief
  frontmatter for the link once created.
- **Sister piece in workspace:** `../context-is-only-one-layer/` —
  the LinkedIn post that establishes the IQ-stack frame this piece
  develops at the Inference-layer specifically.

## 5. Workflow

Five gates, no skipping (see `tasks.md` for the done-when checklist
per gate):

1. **Outline** — fill plan.md, get human sign-off → THEN draft
   *(plan.md is generated; **awaiting human sign-off before §2 begins**)*
2. **Draft** — write to `draft.mdx` in this directory
3. **Revise** — apply reviewer feedback in same file
4. **Polish** — frontmatter, alt text, cross-links, avoid-this sweep,
   "by design" sweep, audience sweep (legal-ops + corporate-counsel)
5. **Hero** — SVG (default) or photographic prompt (held in reserve)
6. **Ship** — move to `content/blog/2026-05-21-probabilistic-vs-deterministic.mdx`,
   update calendar + Project + Issue, update campaign file

## 6. What you don't do here

- Do not auto-publish. Final publish is human-driven.
- Do not fabricate stats, sources, or quotes. The piece is qualitative
  by design — if a quantitative claim is reached for, mark it
  `**TBD — confirm**` and either source it or remove it.
- Do not skip the outline gate. Do not draft before plan.md is
  approved.
- Do not invent new tag values. Tags must come from the canonical
  taxonomy in `../../voice/taxonomy.md`.
- Do not use "by design" as casual shorthand for the probabilistic
  property — frame it as a consequence of how generative models work
  (next-token prediction sampled from a learned statistical
  distribution). See `brief.md` Voice notes.
- Do not recite "Data → Memory → Inference" — when the IQ stack
  appears in §3, it should organize the paragraph, not be
  name-checked (per `style-guide.md` §5.10).
- Do not pile up analogies. The locked pair is **calculator** +
  **experienced advisor**; weather forecasts, chess engines, and
  senior-associate estimates are tempting but dilute.
